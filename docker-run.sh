# Build the docker image 
docker build -t interview-notes .

# Run the docker image
docker run -p 8080:80 interview-notes

# Now, your Docusaurus site should be accessible at http://localhost.









